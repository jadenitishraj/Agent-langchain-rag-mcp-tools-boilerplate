# =============================================================================
# OpenAI (REQUIRED)
# =============================================================================
OPENAI_API_KEY=sk-proj-xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx

# =============================================================================
# Database (REQUIRED for production)
# Leave blank to use SQLite locally (dev only — not safe for production).
# For production, set a PostgreSQL URL:
# =============================================================================
# DATABASE_URL=postgresql://user:password@localhost:5432/agentforge

# =============================================================================
# CORS — Allowed Frontend Origins (REQUIRED for production)
# Comma-separated list of origins that can call this API.
# Defaults to localhost dev ports if not set.
# =============================================================================
# ALLOWED_ORIGINS=https://your-app.com,https://www.your-app.com

# =============================================================================
# Optional API Keys
# =============================================================================

# Google API Key (Optional - for Maps/Search if added)
# GOOGLE_API_KEY=AIzaSymxxxxxxxxxxxxxxxxxxxxxxxxxxxx

# Brave Search API Key (Optional - for Brave Search MCP)
# Get one at https://brave.com/search/api/
# BRAVE_API_KEY=BSAxxxxxxxxxxxxxxxxxxxxxxxxxxxx

# =============================================================================
# App Settings
# =============================================================================
APP_ENV=development
DEBUG=true
PORT=8000
